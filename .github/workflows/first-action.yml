name: Self-hosted test
 
on:

  push:

    branches: [ main ]
 
jobs:

  build:

    runs-on: self-hosted

    steps:

      - uses: actions/checkout@v4

      - name: Test Script

        run: echo "Runner working on $(uname -a)"
 
